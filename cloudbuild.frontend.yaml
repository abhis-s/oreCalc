steps:
- name: 'node:22-slim'
  entrypoint: 'npx'
  args: ['pnpm', 'install', '--frozen-lockfile']

- name: 'node:22-slim'
  entrypoint: 'npx'
  args: ['pnpm', 'run', 'build']
  env:
  - 'VERBOSE=true'
  - 'TAG_NAME=$TAG_NAME'

- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '--build-arg', 'TAG_NAME=$TAG_NAME', '-t', 'gcr.io/$PROJECT_ID/orecalc-webapp:$COMMIT_SHA', '.']

- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/$PROJECT_ID/orecalc-webapp:$COMMIT_SHA']

- name: 'gcr.io/cloud-builders/gcloud'
  args: ['run', 'deploy', 'orecalc-webapp',
         '--image', 'gcr.io/$PROJECT_ID/orecalc-webapp:$COMMIT_SHA',
         '--region', 'europe-west1',
         '--allow-unauthenticated',
         '--platform', 'managed',
         '--port', '80']
  entrypoint: gcloud

images:
- 'gcr.io/$PROJECT_ID/orecalc-webapp:$COMMIT_SHA'
