{
  "dependencies": {
    "express": "^5.1.0",
    "workbox-window": "^7.3.0"
  },
  "name": "orecoc",
  "version": "1.0.0",
  "description": "",
  "main": "service-worker.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "node scripts/dev/copy-workbox.js && node -r dotenv/config scripts/inject-env.js && concurrently \"node scripts/dev/watch-sass.js\" \"node scripts/dev/start-dev-server.js\"",
    "build": "VITE_API_BASE_URL=https://api.orecalc.tech node scripts/build.js && npm run build:css && npm run build:sw",
    "build:css": "sass css/main.scss dist/css/main.min.css --style compressed",
    "build:sw": "npx workbox-cli generateSW workbox-config.js",
    "start:prod": "node scripts/prod/start-prod-server.js"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.0",
    "cpx": "^1.5.0",
    "dotenv": "^17.2.1",
    "live-server": "^1.2.2",
    "replace-in-file": "^8.3.0",
    "sass": "^1.89.2",
    "sharp": "^0.34.3",
    "terser": "5.43.1",
    "workbox-cli": "^7.3.0"
  }
}
